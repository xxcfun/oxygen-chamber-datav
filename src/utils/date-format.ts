import dayjs from 'dayjs'import utc from 'dayjs/plugin/utc'dayjs.extend(utc)const DATE_TIME_FORMAT = 'YYYY-MM-DD HH:mm'export function formatUtcString(utcString: string, format: string = DATE_TIME_FORMAT) {  return dayjs.utc(utcString).utcOffset(8).format(format)}const DATE_TIME_YYYY = 'YYYY-MM-DD'export function formatUtcYYYY(utcString: string, format: string = DATE_TIME_YYYY) {  return dayjs.utc(utcString).utcOffset(8).format(format)}const DATE_TIME_HH = 'HH:mm'export function formatUtcHH(utcString: string, format: string = DATE_TIME_HH) {  return dayjs.utc(utcString).utcOffset(8).format(format)}